name: eoon_app
description: "Application de navigation EooN"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  maplibre_gl: ^0.16.0
  geolocator: ^10.1.0
  permission_handler: ^11.0.1
  http: ^1.2.0
  web_socket_channel: ^3.0.0 # <--- Ajout pour le temps réel WebSocket

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
